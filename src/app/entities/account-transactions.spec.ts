import { AccountTransactions } from './account-transactions';

describe('AccountTransactions', () => {
  it('should create an instance', () => {
    expect(new AccountTransactions()).toBeTruthy();
  });
});
